# Public Skills (PyPI-bundled)
Universal Skills shipped to every consumer via `pip install concinno[windows-full]`.
Run `concinno-skills` to install them into `~/.claude/skills/public/<name>/` with a CC-discovery junction.
